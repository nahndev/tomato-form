import type { DisplayValueProps } from "@/features/board/components/submission/display/types";

const UnknownValue: React.FC<DisplayValueProps> = () => <span className="truncate">—</span>;

export default UnknownValue;
