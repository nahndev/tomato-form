export const GRID_COLUMNS = 4;
export const COLUMN_WIDTH = 200; // px per column
export const CONTAINER_MIN_HEIGHT = 100; // px
export const DRAGGING_Z_INDEX = 50;
export const COLLISION_DELTA = 0.2; // min vertical overlap (% of smaller rect's height) to count as a collision
