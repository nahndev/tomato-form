# Verify and fix for endpoints for job and emitter

- Currently, the `job.controller` is incorrect (by update)

## Tasks

- [x] Add new schema link `board` to `job`
- [x] Enhance `job` and `emitter` include `controller` and `service`.
- [x] Add controller to
  - Endpoint to create `job` from `board`
  - Endpoint to get all `job` in `board`
- [x] Update UI feature [job]('./website/src/features/board/components/job');
