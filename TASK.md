# Limit GridLayout.column and GridLayout.span to be between 0 and GRID_COLUMNS

## Tasks

- [x] Update the `addWidget` function in `FormBuilder.tsx` to ensure that the `column` and `span` values are between 0 and `GRID_COLUMNS`.
- [x] Update the `updateLayout` function in `FormBuilder.tsx` to ensure that the `column` and `span` values are between 0 and `GRID_COLUMNS`.
