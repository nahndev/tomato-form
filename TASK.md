# Cleanup `useCurrentSessionId`

Because i allow show multiple session, should `useCurrentSessionId` don't working well

## Tasks

- [x] Remove hooks `useCurrentSessionId`
- [x] Remove all setting and code related with `useCurrentSessionId`
