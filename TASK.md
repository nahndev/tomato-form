# Remove `Job`

## Currently

- The project include `job` feature, but it don't compatible with my system
- We will implement that feature in the future.

## Tasks

- [ ] Remove `job` and `action` in [website]

- [ ] Remove `job` and `job-action` and `cron` in [server]
